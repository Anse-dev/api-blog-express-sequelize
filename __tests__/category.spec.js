const { check } = require("express-validator");

describe("Test category Route", () => {
  it("GET/ALL_CATEGORIES", () => {});
  it("GET/ONE_CATEGORY", () => {});
  it("UPDATE/ONE_CATEGORY", () => {});
  it("ERROR WHEN ID OF CATEGORY/ONE_CATEGORY", () => {});
  it("ADD/ONE_CATEGORY", () => {});
  it("DELETE/ONE_CATEGORY", () => {});
});
